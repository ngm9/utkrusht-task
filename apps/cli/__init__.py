"""Unified Utkrusht task-tooling CLI.

Entry point: ``python -m apps.cli`` (a Click group). Replaces the old
``multiagent.py`` shim and the top-level ``cli/`` package. Deploy / reset live in
``python -m infra.e2b``.
"""
